import styles from './HeaderComponent.module.css';

const HeaderComponent = () => {
  return (
    <header>
      <div className={styles.test}>HeaderComponent by me</div>{' '}
    </header>
  );
};

export default HeaderComponent;
